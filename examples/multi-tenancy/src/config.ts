import type { ThenaConfig } from "@thenajs/core";

export const config: ThenaConfig = {
  // Ligue para ver a árvore da execução ao vivo. Desligado aqui só para a
  // saída do exemplo ficar limpa.
  log: false,
};
